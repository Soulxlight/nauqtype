enum Flag {
    Enabled,
    Disabled,
}

fn score(flag: Flag) -> i32 {
    match flag {
        Enabled => {
            return 1;
        },
        Disabled => {
            return 0;
        },
    }
}

fn main() -> i32 {
    return score(Enabled);
}

