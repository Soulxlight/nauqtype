pub type Box {
    value: i32,
}

pub enum Choice {
    Keep(Box),
    Skip,
}

pub fn make_box(value: i32) -> Box
audit {
    intent("Construct a boxed value for the import-alias example");
    mutates();
    effects();
}
{
    return Box { value: value };
}
