type Point {
    x: i32,
    y: i32,
}

fn shift_x(point: Point, next_x: i32) -> Point {
    return Point { from point, x: next_x };
}

fn main() -> i32 {
    let start = Point { x: 5, y: 37 };
    let moved = shift_x(start, 10);
    return moved.x + moved.y - 5;
}
