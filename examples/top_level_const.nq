pub const answer: i32 = 40 + 2;
const greeting: str = "Hello from top-level const!";
const should_print: bool = true and not false;

fn main() -> i32 {
    if should_print {
        print_line(greeting);
    }
    return answer - 42;
}
