type User {
    name: str,
    age: i32,
}

fn age_of(user: ref User) -> i32 {
    return user.age;
}

fn main() -> i32 {
    let user = User { name: "Perry", age: 32 };
    return age_of(ref user);
}

