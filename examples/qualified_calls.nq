use batch_b_helper;

fn main() -> i32 {
    let first = batch_b_helper::add_pair(right: 2, left: 40);
    let adjustment = batch_b_helper::choose(no: 1, yes: 0, flag: first == 42);
    return first + adjustment;
}
