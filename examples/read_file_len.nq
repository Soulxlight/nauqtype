fn main() -> i32 {
    let data = read_file("read_file_len_input.txt");
    match data {
        Ok(text) => {
            return str_len(text);
        },
        Err(err) => {
            print_line(io_err_text(err));
            return 1;
        },
    }
}
