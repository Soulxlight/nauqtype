name: Linux Alpha

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  linux-alpha:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Linux alpha gate
        run: scripts/check_linux_alpha.sh
