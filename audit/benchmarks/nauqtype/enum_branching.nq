enum Flag {
    Enabled,
    Disabled,
}

fn score(flag: Flag) -> i32 {
    match flag {
        Enabled => {
            return 1;
        },
        Disabled => {
            return 0;
        },
    }
}

