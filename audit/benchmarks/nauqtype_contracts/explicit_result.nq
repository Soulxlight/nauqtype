fn parse_flag(text: str) -> result<bool, str>
audit {
    intent("Parse yes into true or return an error");
    mutates();
    effects();
}
{
    if text == "yes" {
        return Ok(true);
    } else {
        return Err("expected yes");
    }
}
