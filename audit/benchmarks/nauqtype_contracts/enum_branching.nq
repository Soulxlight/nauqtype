enum Flag {
    Enabled,
    Disabled,
}

fn score(flag: Flag) -> i32
audit {
    intent("Map a flag to a numeric score");
    mutates();
    effects();
}
{
    match flag {
        Enabled => {
            return 1;
        },
        Disabled => {
            return 0;
        },
    }
}
