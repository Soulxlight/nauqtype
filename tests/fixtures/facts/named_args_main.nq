use named_args_helper;

fn local(left: i32, right: i32) -> i32 {
    return left + right;
}

fn main() -> i32 {
    let subtotal = local(right: 2, left: 1);
    return combine(scale: 10, right: subtotal, left: 1);
}
