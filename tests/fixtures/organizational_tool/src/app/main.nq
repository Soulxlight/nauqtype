use reporting::render;

fn main() -> i32
audit {
    intent("Print a checked readiness report from the locked reporting workspace.");
    mutates();
    effects(print);
}
{
    print_line(reporting::render::status_line());
    return 0;
}
