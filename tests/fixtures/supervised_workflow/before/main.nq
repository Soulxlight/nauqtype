pub fn apply(value: i32) -> i32
audit {
    intent("Return the reviewed value");
    mutates();
    effects();
}
{
    return value;
}

pub fn main() -> i32
audit {
    intent("Return the reviewed value");
    mutates();
    effects();
}
{
    let value: i32 = apply(40);
    return value;
}
