use app::model as model;

fn main() -> i32 {
    let boxed = model::make_box(7);
    let direct = model::Box { value: boxed.value };
    let choice = model::Keep(direct);
    match choice {
        model::Keep(value) => { return value.value; },
        model::Skip => { return 0; },
    }
}
