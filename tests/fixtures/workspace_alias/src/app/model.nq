pub type Box {
    value: i32,
}

pub enum Choice {
    Keep(Box),
    Skip,
}

pub fn make_box(value: i32) -> Box
audit {
    intent("Construct a box for the explicit module-alias fixture");
    mutates();
    effects();
}
{
    return Box { value: value };
}
