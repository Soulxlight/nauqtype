fn inspect(items: mutref list<i32>) -> i32
audit {
    intent("Detect a declaration not supported by complete read-only call evidence.");
    mutates(items);
    effects();
}
{
    return list_len(ref items);
}

fn main() -> i32 { return 0; }
