use mutation_helper as writer;

fn forward(target: mutref i32) -> unit
audit {
    intent("Reject an omitted imported mutation.");
    mutates();
    effects();
}
{
    writer::write(target: mutref target, amount: 1);
    return;
}

fn main() -> i32 { return 0; }
