fn append(items: mutref list<i32>) -> unit
audit {
    intent("Reject an omitted builtin-mediated mutation.");
    mutates();
    effects();
}
{
    list_push(mutref items, 1);
    return;
}

fn main() -> i32 {
    let mut items: list<i32> = list();
    append(mutref items);
    print_line("M54_8_SHOULD_NOT_RUN");
    return 0;
}
