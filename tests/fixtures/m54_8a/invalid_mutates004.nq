fn read_value(value: i32) -> i32
audit {
    intent("Declare mutation of a non-mutref parameter.");
    mutates(value);
    effects();
}
{
    return value;
}

fn main() -> i32
audit {
    intent("Call the invalid mutation-contract helper.");
    mutates();
    effects();
}
{
    return read_value(1);
}
