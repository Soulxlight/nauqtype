pub enum PublicErr {
    Failed,
}

pub fn fail_imported() -> result<i32, PublicErr>
audit {
    intent("Return an imported public error result.");
    mutates();
    effects();
}
{
    return Err(Failed);
}
