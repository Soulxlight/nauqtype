pub fn helper() -> unit
audit {
    intent("Print a helper line");
    mutates();
    effects(print);
}
{
    print_line("hi");
    return;
}

fn main() -> i32
audit {
    intent("Call helper");
    mutates();
    effects(print);
}
{
    helper();
    return 0;
}
