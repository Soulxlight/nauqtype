type Cargo {
    values: list<i32>,
}

fn main() -> i32 {
    let cargoes: list<Cargo> = [Cargo { values: [1, 2] }];
    for cargo in cargoes {
        return list_len(ref cargo.values);
    }
    return 0;
}
