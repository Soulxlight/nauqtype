fn consume(values: list<i32>) -> i32 {
    return list_len(ref values);
}

fn main() -> i32 {
    let values: list<i32> = [1, 2, 3];
    let measured = consume(values);
    return measured + list_len(ref values);
}
