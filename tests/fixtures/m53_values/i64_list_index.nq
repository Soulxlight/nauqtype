fn main() -> i32 {
    let values: list<i32> = [7];
    let index: i64 = 0;
    let item = list_get(ref values, index);
    match item {
        Some(value) => {
            return value;
        },
        None => {
            return 0;
        },
    }
}
