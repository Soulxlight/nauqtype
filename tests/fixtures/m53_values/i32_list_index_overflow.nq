fn main() -> i32 {
    let values: list<i32> = [7];
    let item = list_get(ref values, 2147483648);
    match item {
        Some(value) => {
            return value;
        },
        None => {
            return 0;
        },
    }
}
