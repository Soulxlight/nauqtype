fn consume(data: bytes) -> i64 {
    return bytes_len(ref data);
}

fn main() -> i32 {
    let data = bytes_from_str("abc");
    let measured = consume(data);
    if measured != 3 {
        return 1;
    }
    if bytes_len(ref data) != 3 {
        return 2;
    }
    return 0;
}
