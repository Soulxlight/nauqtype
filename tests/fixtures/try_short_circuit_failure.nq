fn ready() -> result<bool, io_err> {
    return Ok(true);
}

fn main() -> i32 {
    let outcome: result<bool, io_err> = try {
        false and ready()?
    };
    match outcome {
        Ok(_) => {
            return 0;
        },
        Err(_) => {
            return 1;
        },
    }
}
