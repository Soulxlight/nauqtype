type Job { child: process, }
pub fn discard(value: option<Job>) -> unit
audit { intent("This declaration intentionally omits owned cleanup authority"); mutates(); effects(); }
{ return; }
fn main() -> i32 { return 0; }
