pub const b__VALUE: i32 = 5;

pub type b__Box {
    int: str,
}

pub fn b__f() -> i32
audit {
    intent("Keep a source double underscore distinct from a module boundary.");
    mutates();
    effects();
}
{
    return b__VALUE;
}

pub fn flat_boxed() -> result<b__Box, str>
audit {
    intent("Exercise a generic carrier over an underscore-named product type.");
    mutates();
    effects();
}
{
    return Ok(b__Box { int: str_concat("de", "fgh") });
}
