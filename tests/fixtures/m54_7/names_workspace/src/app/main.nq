use a::b as nested;
use a as flat;

fn main() -> i32 {
    if nested::f() != 3 or flat::b__f() != 5 {
        return 1;
    }
    let Some(left) = nested::nested_boxed() else {
        return 2;
    };
    let Ok(right) = flat::flat_boxed() else {
        return 3;
    };
    let lefts = [left];
    let rights = [right];
    let Some(left_again) = list_get(ref lefts, 0) else {
        return 4;
    };
    let Some(right_again) = list_get(ref rights, 0) else {
        return 5;
    };
    if left_again.int != "abc" or right_again.int != "defgh" {
        return 6;
    }
    return 0;
}
