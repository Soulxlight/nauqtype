use reporting::values;

pub fn answer() -> i32
audit {
    intent("Return the reporting value through the explicit dependency module path.");
    mutates();
    effects();
}
{
    return reporting::values::value();
}
