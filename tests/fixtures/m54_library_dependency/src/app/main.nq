use std::status;

fn main() -> i32
audit {
    intent("Exercise a locked vendored Nauqtype library through its public nominal value.");
    mutates();
    effects(print);
}
{
    let status = std::status::ready_status();
    print_line(status.message);
    return status.code;
}
